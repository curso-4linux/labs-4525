description: Viewer, visualize all jobs.
context:
  application: 'rundeck'
for:
  project:		# permite visualizar o projeto na interface do Rundeck
  - equals:
      name: 'infra-agil'
    allow: 'read'	# Podemos definir vários em formato de array ou utilizar um asterisco.
  storage:		# Permite acessar usuários e senhas definidos no Rundeck
       Rundeck
  - equals:
      name: 'infra-agil'
    allow: 'read'
by:
group: viewer
---
description: Viewer, visualize all jobs.
context:
  project: 'infra-agil'	# define políticas específicas deste projeto
for:
  resource: 		# um resource abrange recursos que pode ainda não existir, são mais genéricos
  - allow: 'read' 	# os recursos mais específicos abaixo precisam estar permitidos em resource
  adhoc:		# controle dos acessos aos comandos	
  - allow: [read,run]	# execução e leitura dos resultados
  job:			# controle dos acessos aos jobs
  - allow: [read,run]	# execução e leitura dos resultados
  node:			# controle dos acessos aos nodes
  - allow: [read,run]	# execução de jobs, comandos e visualização em nodes específicos
by:
  group: viewer
